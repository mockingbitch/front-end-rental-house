// eslint-disable-next-line react/prop-types
const Layout = ({children}) => {
    return (
        <>
            <h1>

            </h1>
            { children }
        </>
    );
}

export default Layout;